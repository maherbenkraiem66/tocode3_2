In this scenarion, the user will introduce the id of the destination existing in our database,
the first web service which is the local one , will fetch the row according to the id introduced, and we will use
the country name , finally we get the additionnal data from the external web service via the country name
we had from our local database and local web service.